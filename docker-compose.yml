name: pull-requests

services:
  app:
    image: pull-requests:1.0
    build: .
    environment:
      - MONGODB_URI=mongodb://user:pass@mongo
    ports:
      - 3000:3000
    volumes:
      - ./:/app
      - ./public/assets:/app/public/assets
      - /app/node_modules
volumes:
  data-volume:
